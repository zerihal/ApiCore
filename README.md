## API Core

Provides a flexbile core web API (ASP.NET) that pulls in additional API projects that contain a service (preferred) or other class that implements the IApiModule interface. Currently contains sub-APIs for document analysis and code analysis.
